# ── 供应商兼容层 ─────────────────────────────────────────
# TTS: edge-tts（免费，无需 Key）| mimo（完整能力）| openai_compatible（experimental）
# LLM: freellmapi / openai（supported）| ollama / custom（experimental）
# 完整说明见 README「供应商兼容层」与 backend/app/providers/catalog.py
# 未设置 TTS_PROVIDER 时：有 MIMO_API_KEY 则用 mimo，否则自动用 edge-tts

# TTS_PROVIDER=edge-tts
# MIMO_API_KEY=your_mimo_api_key
# MIMO_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1

# 写稿用 LLM（OpenAI 兼容）
LLM_PROVIDER=freellmapi
LLM_API_KEY=
LLM_BASE_URL=http://localhost:3001/v1
LLM_MODEL=auto
