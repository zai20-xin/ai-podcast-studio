# 变更日志

## [1.3.0] - 2026-09

### Added
- **场景预设导演化**：12 个预设改写为「角色 / 场景 / 指导」结构的连贯情境描述，
  替代原有关键词罗列式描述
- **停顿分级**：句间(350ms) / 段落(900ms) / 章节(1400ms)。层级由脚本结构自动决定
  （空行 = 段落，【章节】= 章节），可用 `SILENCE_GAP_*` 环境变量调整。
  单一停顿长度是「AI 播客听起来平」的主要来源之一
- **一键演绎化**：AI 把干稿重排成演播稿——拆短长句、在情绪落点加语气标签、整理段落，
  并在覆盖前弹确认。零基础用户不再需要懂「在哪插什么标签」
- 高级参数（风格 / 语速 / 句首标签 / 情绪）折叠进「高级设置」，默认收起；
  手动改动过会在折叠区标题显示「已自定义」
- `scripts/ab_compare.py`：新旧指令 A/B 对比试听工具。两版用同一种音色合成，
  唯一变量是指令文本，听到的差异全部来自指令

### Fixed
- **指令自相矛盾**：旧实现把场景描述、风格字典、语速字典三处对语速的不同要求
  拼进同一条指令——「睡前陪伴」场景的语速被描述 3 次且强度互斥
  （非常慢 / 偏慢 / 非常缓慢），模型行为不可预测。改为维度职责分离：
  情境描述只管「怎么演」、语速只由语速字典负责，任一预设的指令中语速只出现 1 次
- 14 条风格描述不再各自声称语速（旧版 14 条中有 12 条自带语速表述）
- 商业财经 / 轻松闲聊预设改用中文音色（原先配的是英文音色 Dean / Mia，
  中文内容用英文音色会违和）
- 情绪从标签式 `情绪：平静` 改为自然语言 `整体情绪偏平静`

## [1.2.0] - 2026-09

### Added
- 合成可取消：新增「停止」按钮与 `POST /api/podcast/episodes/{id}/cancel`。
  中断的是后端任务本身（不再继续消耗 API 额度），已完成的分句保留，可随时续跑
- 分句并发合成：实际在飞请求数由并发闸门约束（`TTS_CONCURRENCY`，默认 4）
- 限流退避：遇到 429 时指数退避自动重试（`TTS_RATE_LIMIT_*`），退避期间仍可取消
- 中间产物自动清理：启动时与每 6 小时清理过期试听缓存（`INTERMEDIATE_KEEP_HOURS`）
- 超长脚本显式报错（上限 200 句），不再静默丢弃超出部分

### Fixed
- **增量复用完全失效**：合成前清空 `segments_json`，导致复用判断的缓存来源永远为空，
  改一个字也会整篇重新合成。现按「配置指纹」（音色 / 风格 / 参考音频 / 全局指令 / 文本）
  逐句判断能否复用
- **SRT 时间轴错位**：字幕未计入句间 600ms 停顿与片头时长，偏移随句数线性累积
  （50 句时约偏 30 秒）。现与合并逻辑完全对齐
- **合并性能**：`combined += seg` 是 O(n²) 数据拷贝，段数越多越慢；改为单次拼接
- **改稿丢失重试上下文**：脚本一编辑就清空分句状态，连带「仅重试失败句」入口一起消失；
  失败清单改为独立快照
- **草稿复活**：合成成功后清空的草稿会在离开页面时被重新写回
- **单句失败阻塞全篇**：改为继续合成其余句子并汇总失败，连续 3 句失败才判定为系统性问题中止
- 参考音频大小校验改为按 Base64 编码后口径（官方限制），原始文件上限相应折算为约 7.5MB
- 克隆音色参考音频丢失时返回 400（原实现漏成 500）
- 片头片尾补齐 `audio_tag_style` 传递

### Changed
- `synthesize` / `retry` 不再使用 Starlette BackgroundTasks（拿不到 task 引用，无法取消）

## [1.1.0] - 2026-09

### Added
- 分句合成与失败续跑、单句试听、草稿自动保存
- 合成前预估确认；片头片尾；SRT / 脚本 Markdown 导出
- AI 写稿与口语化 / 压短 / 润色（OpenAI 兼容 LLM，可在设置中配置）
- 声音设计模型、克隆音色管理与试听
- 项目列表统计；错误信息人话化

### Security
- 默认监听 127.0.0.1；CORS 收紧为本地前端源
- 密钥仅经环境变量 / `.env`（已 ignore），示例文件不含真实 Key

### Docs
- 补充 LICENSE / SECURITY / CONTRIBUTING 与完整 README
