# Security Policy

## 不要把密钥提交进仓库

- 本仓库通过环境变量 / `backend/.env`（已在 `.gitignore`）读取密钥
- 请只提交 `backend/.env.example`，其中不得写入真实 Key
- 若曾提交过密钥，请立即在服务商控制台**轮换作废**

## 默认部署形态

当前实现面向**本机 / 受信内网**使用：

- API **无鉴权**
- CORS 默认放开（开发便利）
- 后端默认监听本机端口

**不要**直接把服务暴露到公网。若需对外提供，请自行加上鉴权、收紧 CORS、使用 HTTPS 反向代理，并限制文件目录访问。

## 报告漏洞

请通过 GitHub Issues（若已开源）或私下联系维护者报告，勿在公开 Issue 中粘贴密钥或可利用的完整 payload。
